galaxy_info:
  namespace: maxvalue
  role_name: sshd
  description: Configure the openssh server process
  author: Maximilian Fuxjäger

  issue_tracker_url: https://gitlab.com/maxvalue-ansible/ansible-role-sshd

  license: MIT

  min_ansible_version: "2.1"

  galaxy_tags:
    - security
    - ssh
    - installer
    - install
    - sudo
    - sysadmin
    - setup
    - base
    - installation
    - network
    - hardening

dependencies: []
